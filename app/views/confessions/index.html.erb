<%= turbo_frame_tag "confessions_list" do %>
  <div class="p-4 max-w-6xl mx-auto mt-4">
    <% if @confessions.any? %>
      <div id="confessions_grid" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        <%= render partial: "confessions/confession", collection: @confessions, as: :confession %>
      </div>
    <% else %>
      <div class="text-center text-gray-500 text-lg py-12">
        🕊️ Be the first to share a confession!
      </div>
    <% end %>
  </div>
<% end %>
